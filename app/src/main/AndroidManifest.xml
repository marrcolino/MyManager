<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mymanager">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyManager">
        <activity android:name=".Lista_gruppi_iscritti"></activity>
        <activity android:name=".InfoProfCasoDiStudio" />
        <activity android:name=".InfoGruppo" />
        <activity android:name=".InfoCasoDiStudio" />
        <activity android:name=".CreaGruppo" />
        <activity android:name=".CreaCasoDiStudio" />
        <activity
            android:name=".Home"
            android:label="@string/title_activity_home"
            android:theme="@style/Theme.MyManager.NoActionBar" />
        <activity android:name=".Registrazione" />
        <activity android:name=".Login" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>